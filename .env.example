# Your deployed indexer GraphQL endpoint
# Required for running GraphQL integration tests (pnpm test:queries)
# If not set, the integration tests will be automatically skipped
GRAPH_API_URL=https://your-indexer-endpoint.envio.dev/graphql


# To create or update a token visit https://envio.dev/app/api-tokens
ENVIO_API_TOKEN="<YOUR-API-TOKEN>"

# Database credentials—presumably useful for `pnpm envio local db-migrate up`
ENVIO_PG_DATABASE=enviodb
ENVIO_PG_HOST=0.0.0.0
ENVIO_PG_PASSWORD=enviopassword
ENVIO_PG_PORT=5433
ENVIO_PG_USER=enviouser
